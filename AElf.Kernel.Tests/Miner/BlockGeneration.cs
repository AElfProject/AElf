using AElf.Kernel.Managers;
using AElf.Kernel.Services;

namespace AElf.Kernel.Tests.Miner
{
    /*public class BlockGeneration
    {
        private readonly IWorldStateManager _worldStateManager;
        private readonly IChainManager _chainManager;
        private readonly IBlockManager _blockManager;

        public BlockGeneration(IBlockManager blockManager, IChainManager chainManager, IWorldStateManager worldStateManager)
        {
            _blockManager = blockManager;
            _chainManager = chainManager;
            _worldStateManager = worldStateManager;
        }

        public void GenerateBlock()
        {
            var blockGenerationService = new BlockGenerationService(_worldStateManager, _chainManager, _blockManager);

            var chainId = Hash.Generate();
            
            //var block = await blockGenerationService.GenerateBlockAsync(chainId, )；
        }
    }*/
}