using System;
using System.Reflection.Metadata.Ecma335;
using System.Threading.Tasks;
using AElf.Network;
using AElf.Network.Config;
using AElf.Network.Data;
using AElf.Network.Exceptions;
using AElf.Network.Peers;
using Xunit;

namespace AElf.Kernel.Tests.Network.Server
{
    public class PeerManagerUnitTests
    {
//        [Theory]
//        [InlineData(null, "127.0.0.1", 6900)]
//        public async Task PM_ShouldDrop_Bootnode(NodeData nodeData, string serverHost, int serverPort, string bootnodeHost, string bootnodePort)
//        {
//            IAElfNetworkConfig _config = new AElfNetworkConfig
//            {
//                Host = serverHost,
//                Port = serverPort
//            };
//            IAElfServer _server = new AElfTcpServer(_config, null);
//            PeerManager _peerManager = new PeerManager(_server, null, _config, null);
//            
//            Exception ex =
//                await Assert.Contains(NodeData, List<NodeData>)
//        }
    }
}