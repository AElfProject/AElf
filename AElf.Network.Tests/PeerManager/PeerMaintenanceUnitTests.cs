using System.Threading.Tasks;
using AElf.Network.Data;
using Xunit;

namespace AElf.Network.Tests.PeerManager
{
    public class PeerMaintenanceUnitTests
    {
        [Theory]
        public async Task PM_ShouldDrop_Bootnode(NodeData nodeData, string bootnodeHost, string bootnodePort)
        {
            /*IAElfNetworkConfig _config = new AElfNetworkConfig { Host = 127.0.01, Port = serverPort
            };*/
            
            /*IAElfServer _server = new AElfTcpServer(_config, null);
            
            
            
            PeerManager _peerManager = new PeerManager(_server, null, _config, null);
            
            Exception ex =
                await Assert.Contains(NodeData, List<NodeData>)*/
        }
    }
}