#!/usr/bin/env bash
CAKE_VERSION=0.37.0
DOTNET_VERSION=3.1.102
VERSION_PREFIX=0.9.3
BUILD_VERSION=`expr $(date +%s) / 86400`
VERSION=${VERSION_PREFIX}-${BUILD_VERSION}
sed -i "s/VERSION/$VERSION/g" build.cake
